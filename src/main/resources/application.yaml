server:
  port: 8081
  servlet:
    context-path: /todo_list

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/todo-app"
    username: root
    password: abc123-
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
